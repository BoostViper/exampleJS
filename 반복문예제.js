// 1번

function calculate(number) {
  const numData = [];
  for (let i = 0; i <= bigData.length; i++) {
    if ( i > 3 ) {
      numData.push(i);
    }
  }
  return numData;
};

const bigData = [1,2,3,4,5,6,7];
console.log(calculate(number));